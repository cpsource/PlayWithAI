#!/bin/bash

# this is how we save a venv
# pip freeze > requirements.txt

# this is how we restore a venv
# pip install -r requirements.txt

# here's how we make a new venv
# python -m venv venv

# then to run it
# source venv/bin/activate
